// Package api implements the OpenAI-compatible HTTP gateway: chat
// completions (streaming and non-streaming) with automatic model routing,
// plus endpoints for keys, preferences, recommendations, stats, and health.
package api
